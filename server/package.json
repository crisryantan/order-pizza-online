
{
  "name": "order-pizza-server",
  "version": "1.0.0",
  "description": "A server for Ordering Pizza",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "license": "ISC",
  "dependencies": {
    "hapi": "^16.6.2"
  }
}